# Copy this file to .streamlit/secrets.toml for local use (it's gitignored).
# On Streamlit Community Cloud, paste the same content into
# App settings -> Secrets instead of using this file.

API_URL = "http://localhost:8000"
# After deploying the backend to Render, change this to something like:
# API_URL = "https://finpulse-backend.onrender.com"
